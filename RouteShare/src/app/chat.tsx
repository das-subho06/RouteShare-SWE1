import { View, Text } from 'react-native';
import ChatApp from '@/components/Rider/Chat/ChatApp';
export default function ChatComponent() {
  return (
    <View style={{ flex: 1 }}>
      <ChatApp />
    </View>
  );
}